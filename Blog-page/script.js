new WOW().init();

let options = {
    strings: ["A Winters Morning"],
    typeSpeed: 100,
    backSpeed: 50,
    backDelay: 1000,
    startDelay: 500,
    loop: true,
    loop: true,
    showCursor: false,
};

let typed = new Typed("#title", options);